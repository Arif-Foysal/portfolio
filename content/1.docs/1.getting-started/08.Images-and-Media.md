---
title: "Images and Media"
description: "Handle images, videos, and media assets efficiently in React Native applications"
navigation:
  icon: i-lucide-house
---

# Images and Media

#react-native #images #media #assets #stage-2

> Handle images, videos, and media assets efficiently in React Native applications

## 🔗 Navigation
← [[7.Lists and Data Display]] | → [[9.Local State Management]]

---

## Image Component Fundamentals

The Image component is essential for displaying images from various sources.

```jsx path=null start=null
import React from 'react';
import { View, Image, StyleSheet, ScrollView } from 'react-native';

const BasicImageExamples = () => {
  return (
    <ScrollView style={styles.container}>
      {/* Local/Static Images */}
      <Image
        source={require('../assets/logo.png')}
        style={styles.localImage}
      />

      {/* Remote Images */}
      <Image
        source={{ uri: 'https://picsum.photos/300/200' }}
        style={styles.remoteImage}
        resizeMode="cover"
      />

      {/* Image with loading placeholder */}
      <Image
        source={{ uri: 'https://picsum.photos/300/200?random=1' }}
        defaultSource={require('../assets/placeholder.png')}
        style={styles.imageWithPlaceholder}
        onLoadStart={() => console.log('Load started')}
        onLoad={() => console.log('Load finished')}
        onError={() => console.log('Load error')}
      />

      {/* Image with different resize modes */}
      <View style={styles.resizeModeContainer}>
        <Image
          source={{ uri: 'https://picsum.photos/400/300' }}
          style={styles.resizeModeImage}
          resizeMode="cover"
        />
        <Image
          source={{ uri: 'https://picsum.photos/400/300' }}
          style={styles.resizeModeImage}
          resizeMode="contain"
        />
        <Image
          source={{ uri: 'https://picsum.photos/400/300' }}
          style={styles.resizeModeImage}
          resizeMode="stretch"
        />
      </View>
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#f5f5f5',
  },
  localImage: {
    width: 200,
    height: 100,
    marginBottom: 20,
    borderRadius: 8,
  },
  remoteImage: {
    width: 300,
    height: 200,
    marginBottom: 20,
    borderRadius: 12,
  },
  imageWithPlaceholder: {
    width: 300,
    height: 200,
    marginBottom: 20,
    borderRadius: 12,
    backgroundColor: '#e0e0e0', // Fallback background
  },
  resizeModeContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
  },
  resizeModeImage: {
    width: 100,
    height: 100,
    marginBottom: 10,
    backgroundColor: '#e0e0e0',
    borderRadius: 8,
  },
});
```

### Image Resize Modes

```jsx path=null start=null
const resizeModes = [
  'cover',    // Scale to fill, may crop
  'contain',  // Scale to fit, no cropping
  'stretch',  // Stretch to fill exactly
  'repeat',   // Tile the image
  'center',   // Center without scaling
];

const ResizeModeDemo = () => {
  return (
    <View style={styles.container}>
      {resizeModes.map((mode) => (
        <View key={mode} style={styles.modeContainer}>
          <Text style={styles.modeTitle}>{mode}</Text>
          <Image
            source={{ uri: 'https://picsum.photos/400/300' }}
            style={styles.demoImage}
            resizeMode={mode}
          />
        </View>
      ))}
    </View>
  );
};
```

---

## Advanced Image Handling

Loading states, error handling, and performance optimizations.

```jsx path=null start=null
import React, { useState } from 'react';
import {
  View,
  Image,
  Text,
  ActivityIndicator,
  StyleSheet,
  TouchableOpacity,
} from 'react-native';

const AdvancedImage = ({ source, style, ...props }) => {
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(false);
  const [imageLoaded, setImageLoaded] = useState(false);

  const handleLoadStart = () => {
    setLoading(true);
    setError(false);
    setImageLoaded(false);
  };

  const handleLoad = () => {
    setLoading(false);
    setImageLoaded(true);
    setError(false);
  };

  const handleError = () => {
    setLoading(false);
    setError(true);
    setImageLoaded(false);
  };

  const retry = () => {
    setError(false);
    setLoading(true);
    // Force re-render by creating new source object
    setImageLoaded(false);
  };

  return (
    <View style={[styles.imageContainer, style]}>
      <Image
        {...props}
        source={source}
        style={[styles.image, style]}
        onLoadStart={handleLoadStart}
        onLoad={handleLoad}
        onError={handleError}
        // Add cache control for remote images
        {...(typeof source === 'object' && source.uri && {
          source: {
            ...source,
            cache: 'force-cache', // or 'default', 'reload', 'no-cache'
          }
        })}
      />

      {/* Loading indicator */}
      {loading && !error && (
        <View style={styles.loadingOverlay}>
          <ActivityIndicator size="small" color="#007AFF" />
        </View>
      )}

      {/* Error state */}
      {error && (
        <TouchableOpacity style={styles.errorOverlay} onPress={retry}>
          <Text style={styles.errorText}>⚠️</Text>
          <Text style={styles.retryText}>Tap to retry</Text>
        </TouchableOpacity>
      )}
    </View>
  );
};

// Usage example
const ImageGallery = () => {
  const images = [
    { id: '1', uri: 'https://picsum.photos/300/200?random=1' },
    { id: '2', uri: 'https://picsum.photos/300/200?random=2' },
    { id: '3', uri: 'https://picsum.photos/300/200?random=3' },
    { id: '4', uri: 'https://picsum.photos/300/200?random=4' },
  ];

  return (
    <ScrollView style={styles.gallery}>
      {images.map((image) => (
        <AdvancedImage
          key={image.id}
          source={{ uri: image.uri }}
          style={styles.galleryImage}
          resizeMode="cover"
        />
      ))}
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  imageContainer: {
    position: 'relative',
  },
  image: {
    width: '100%',
    height: '100%',
  },
  loadingOverlay: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    backgroundColor: 'rgba(255, 255, 255, 0.8)',
    justifyContent: 'center',
    alignItems: 'center',
  },
  errorOverlay: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    backgroundColor: 'rgba(0, 0, 0, 0.1)',
    justifyContent: 'center',
    alignItems: 'center',
  },
  errorText: {
    fontSize: 24,
    marginBottom: 8,
  },
  retryText: {
    fontSize: 14,
    color: '#666',
  },
  gallery: {
    flex: 1,
    padding: 10,
  },
  galleryImage: {
    width: 300,
    height: 200,
    marginBottom: 10,
    borderRadius: 8,
  },
});
```

---

## Image Caching & Optimization

Optimize image loading and caching for better performance.

```jsx path=null start=null
// Using react-native-fast-image for better performance
import FastImage from 'react-native-fast-image';

const OptimizedImage = ({ uri, style, ...props }) => {
  return (
    <FastImage
      style={style}
      source={{
        uri,
        priority: FastImage.priority.normal,
        cache: FastImage.cacheControl.immutable,
      }}
      resizeMode={FastImage.resizeMode.cover}
      onLoadStart={() => console.log('FastImage load started')}
      onLoad={() => console.log('FastImage loaded')}
      onError={() => console.log('FastImage load error')}
      {...props}
    />
  );
};

// Preload images for better user experience
const preloadImages = (imageUrls) => {
  FastImage.preload(
    imageUrls.map(url => ({
      uri: url,
      priority: FastImage.priority.normal,
    }))
  );
};

// Custom hook for image optimization
import { useState, useEffect } from 'react';
import { Dimensions } from 'react-native';

const useOptimizedImageSize = (originalWidth, originalHeight, containerWidth) => {
  const [optimizedSize, setOptimizedSize] = useState({
    width: containerWidth,
    height: containerWidth,
  });

  useEffect(() => {
    if (originalWidth && originalHeight && containerWidth) {
      const aspectRatio = originalHeight / originalWidth;
      const optimizedHeight = containerWidth * aspectRatio;
      
      setOptimizedSize({
        width: containerWidth,
        height: optimizedHeight,
      });
    }
  }, [originalWidth, originalHeight, containerWidth]);

  return optimizedSize;
};

// Progressive image loading
const ProgressiveImage = ({ thumbnailUri, imageUri, style, ...props }) => {
  const [imageLoaded, setImageLoaded] = useState(false);
  const [thumbnailLoaded, setThumbnailLoaded] = useState(false);

  return (
    <View style={style}>
      {/* Low quality thumbnail */}
      <Image
        source={{ uri: thumbnailUri }}
        style={[styles.imageBackground, { opacity: thumbnailLoaded ? 1 : 0 }]}
        onLoad={() => setThumbnailLoaded(true)}
        blurRadius={1} // Slight blur for thumbnail
      />
      
      {/* High quality image */}
      <Image
        source={{ uri: imageUri }}
        style={[styles.imageBackground, { opacity: imageLoaded ? 1 : 0 }]}
        onLoad={() => setImageLoaded(true)}
        {...props}
      />
    </View>
  );
};

const styles = StyleSheet.create({
  imageBackground: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});
```

---

## Image Picker & Camera Integration

Allow users to select images from gallery or take photos.

```jsx path=null start=null
// Using react-native-image-picker
import {
  launchImageLibrary,
  launchCamera,
  MediaType,
} from 'react-native-image-picker';
import { request, PERMISSIONS, RESULTS } from 'react-native-permissions';

const ImagePickerComponent = () => {
  const [selectedImage, setSelectedImage] = useState(null);
  const [loading, setLoading] = useState(false);

  const requestCameraPermission = async () => {
    const permission = Platform.OS === 'ios' 
      ? PERMISSIONS.IOS.CAMERA 
      : PERMISSIONS.ANDROID.CAMERA;

    const result = await request(permission);
    return result === RESULTS.GRANTED;
  };

  const requestGalleryPermission = async () => {
    const permission = Platform.OS === 'ios'
      ? PERMISSIONS.IOS.PHOTO_LIBRARY
      : PERMISSIONS.ANDROID.READ_EXTERNAL_STORAGE;

    const result = await request(permission);
    return result === RESULTS.GRANTED;
  };

  const showImagePicker = () => {
    const options = {
      title: 'Select Image',
      cancelButtonTitle: 'Cancel',
      takePhotoButtonTitle: 'Take Photo',
      chooseFromLibraryButtonTitle: 'Choose from Gallery',
      storageOptions: {
        skipBackup: true,
        path: 'images',
      },
      mediaType: 'photo',
      maxWidth: 1000,
      maxHeight: 1000,
      quality: 0.8,
    };

    // For newer versions, use Alert to show options
    Alert.alert(
      'Select Image',
      'Choose an option',
      [
        { text: 'Camera', onPress: openCamera },
        { text: 'Gallery', onPress: openGallery },
        { text: 'Cancel', style: 'cancel' },
      ]
    );
  };

  const openCamera = async () => {
    const hasPermission = await requestCameraPermission();
    if (!hasPermission) {
      Alert.alert('Permission denied', 'Camera permission is required');
      return;
    }

    const options = {
      mediaType: 'photo',
      includeBase64: false,
      maxHeight: 1000,
      maxWidth: 1000,
      quality: 0.8,
    };

    setLoading(true);
    launchCamera(options, (response) => {
      setLoading(false);
      if (response.assets && response.assets[0]) {
        setSelectedImage(response.assets[0]);
      }
    });
  };

  const openGallery = async () => {
    const hasPermission = await requestGalleryPermission();
    if (!hasPermission) {
      Alert.alert('Permission denied', 'Gallery permission is required');
      return;
    }

    const options = {
      mediaType: 'photo',
      includeBase64: false,
      maxHeight: 1000,
      maxWidth: 1000,
      quality: 0.8,
      selectionLimit: 1, // Allow single selection
    };

    setLoading(true);
    launchImageLibrary(options, (response) => {
      setLoading(false);
      if (response.assets && response.assets[0]) {
        setSelectedImage(response.assets[0]);
      }
    });
  };

  const removeImage = () => {
    setSelectedImage(null);
  };

  return (
    <View style={styles.container}>
      <View style={styles.imageContainer}>
        {selectedImage ? (
          <View style={styles.selectedImageContainer}>
            <Image
              source={{ uri: selectedImage.uri }}
              style={styles.selectedImage}
              resizeMode="cover"
            />
            <TouchableOpacity style={styles.removeButton} onPress={removeImage}>
              <Text style={styles.removeButtonText}>×</Text>
            </TouchableOpacity>
          </View>
        ) : (
          <TouchableOpacity
            style={styles.placeholderContainer}
            onPress={showImagePicker}
            disabled={loading}
          >
            {loading ? (
              <ActivityIndicator size="large" color="#007AFF" />
            ) : (
              <>
                <Text style={styles.placeholderIcon}>📷</Text>
                <Text style={styles.placeholderText}>Tap to add image</Text>
              </>
            )}
          </TouchableOpacity>
        )}
      </View>

      {selectedImage && (
        <View style={styles.imageInfo}>
          <Text style={styles.infoText}>
            Size: {Math.round(selectedImage.fileSize / 1024)} KB
          </Text>
          <Text style={styles.infoText}>
            Dimensions: {selectedImage.width} x {selectedImage.height}
          </Text>
        </View>
      )}

      <TouchableOpacity
        style={[styles.button, styles.primaryButton]}
        onPress={showImagePicker}
        disabled={loading}
      >
        <Text style={styles.buttonText}>
          {selectedImage ? 'Change Image' : 'Select Image'}
        </Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#f5f5f5',
  },
  imageContainer: {
    height: 300,
    marginBottom: 20,
  },
  selectedImageContainer: {
    flex: 1,
    position: 'relative',
  },
  selectedImage: {
    flex: 1,
    borderRadius: 12,
  },
  removeButton: {
    position: 'absolute',
    top: 10,
    right: 10,
    backgroundColor: 'rgba(0, 0, 0, 0.6)',
    width: 30,
    height: 30,
    borderRadius: 15,
    justifyContent: 'center',
    alignItems: 'center',
  },
  removeButtonText: {
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold',
  },
  placeholderContainer: {
    flex: 1,
    borderRadius: 12,
    borderWidth: 2,
    borderColor: '#ddd',
    borderStyle: 'dashed',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  placeholderIcon: {
    fontSize: 48,
    marginBottom: 10,
  },
  placeholderText: {
    fontSize: 16,
    color: '#666',
  },
  imageInfo: {
    backgroundColor: '#fff',
    padding: 15,
    borderRadius: 8,
    marginBottom: 20,
  },
  infoText: {
    fontSize: 14,
    color: '#666',
    marginBottom: 5,
  },
  button: {
    height: 50,
    borderRadius: 8,
    justifyContent: 'center',
    alignItems: 'center',
  },
  primaryButton: {
    backgroundColor: '#007AFF',
  },
  buttonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },
});
```

---

## Video Playback

Basic video playback using react-native-video.

```jsx path=null start=null
// Using react-native-video
import Video from 'react-native-video';
import { useState, useRef } from 'react';

const VideoPlayer = ({ source, style }) => {
  const [paused, setPaused] = useState(true);
  const [currentTime, setCurrentTime] = useState(0);
  const [duration, setDuration] = useState(0);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  const videoRef = useRef();

  const onLoad = (data) => {
    setDuration(data.duration);
    setLoading(false);
  };

  const onProgress = (data) => {
    setCurrentTime(data.currentTime);
  };

  const onError = (error) => {
    setError(error);
    setLoading(false);
  };

  const togglePlayPause = () => {
    setPaused(!paused);
  };

  const seekTo = (time) => {
    videoRef.current.seek(time);
  };

  const formatTime = (timeInSeconds) => {
    const minutes = Math.floor(timeInSeconds / 60);
    const seconds = Math.floor(timeInSeconds % 60);
    return `${minutes}:${seconds.toString().padStart(2, '0')}`;
  };

  if (error) {
    return (
      <View style={[styles.videoContainer, style]}>
        <Text style={styles.errorText}>Failed to load video</Text>
      </View>
    );
  }

  return (
    <View style={[styles.videoContainer, style]}>
      <Video
        ref={videoRef}
        source={source}
        style={styles.video}
        paused={paused}
        resizeMode="contain"
        onLoad={onLoad}
        onProgress={onProgress}
        onError={onError}
        onEnd={() => setPaused(true)}
      />

      {/* Loading overlay */}
      {loading && (
        <View style={styles.loadingOverlay}>
          <ActivityIndicator size="large" color="#fff" />
        </View>
      )}

      {/* Controls */}
      <View style={styles.controlsContainer}>
        <TouchableOpacity style={styles.playButton} onPress={togglePlayPause}>
          <Text style={styles.playButtonText}>
            {paused ? '▶️' : '⏸️'}
          </Text>
        </TouchableOpacity>

        <View style={styles.timeContainer}>
          <Text style={styles.timeText}>
            {formatTime(currentTime)} / {formatTime(duration)}
          </Text>
        </View>
      </View>

      {/* Progress bar */}
      <View style={styles.progressContainer}>
        <View style={styles.progressBackground}>
          <View
            style={[
              styles.progressFill,
              {
                width: duration > 0 ? `${(currentTime / duration) * 100}%` : '0%',
              },
            ]}
          />
        </View>
      </View>
    </View>
  );
};

// Usage
const VideoScreen = () => {
  const videos = [
    {
      id: '1',
      title: 'Sample Video 1',
      source: { uri: 'https://www.example.com/video1.mp4' },
    },
    {
      id: '2',
      title: 'Local Video',
      source: require('../assets/sample-video.mp4'),
    },
  ];

  return (
    <ScrollView style={styles.container}>
      {videos.map((video) => (
        <View key={video.id} style={styles.videoSection}>
          <Text style={styles.videoTitle}>{video.title}</Text>
          <VideoPlayer
            source={video.source}
            style={styles.playerContainer}
          />
        </View>
      ))}
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  videoSection: {
    marginBottom: 30,
  },
  videoTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    padding: 15,
  },
  playerContainer: {
    height: 250,
  },
  videoContainer: {
    position: 'relative',
    backgroundColor: '#000',
  },
  video: {
    flex: 1,
  },
  loadingOverlay: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
  },
  controlsContainer: {
    position: 'absolute',
    bottom: 50,
    left: 0,
    right: 0,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 20,
  },
  playButton: {
    padding: 10,
  },
  playButtonText: {
    fontSize: 24,
  },
  timeContainer: {
    flex: 1,
    alignItems: 'center',
  },
  timeText: {
    color: '#fff',
    fontSize: 14,
  },
  progressContainer: {
    position: 'absolute',
    bottom: 20,
    left: 20,
    right: 20,
  },
  progressBackground: {
    height: 4,
    backgroundColor: 'rgba(255, 255, 255, 0.3)',
    borderRadius: 2,
  },
  progressFill: {
    height: '100%',
    backgroundColor: '#007AFF',
    borderRadius: 2,
  },
  errorText: {
    color: '#fff',
    textAlign: 'center',
    fontSize: 16,
  },
});
```

---

## Asset Management & Organization

Best practices for organizing and managing media assets.

```jsx path=null start=null
// assets/index.js - Centralized asset management
export const images = {
  // App branding
  logo: require('./images/logo.png'),
  logoLight: require('./images/logo-light.png'),
  
  // Icons
  homeIcon: require('./icons/home.png'),
  profileIcon: require('./icons/profile.png'),
  settingsIcon: require('./icons/settings.png'),
  
  // Placeholders
  avatarPlaceholder: require('./images/avatar-placeholder.png'),
  imagePlaceholder: require('./images/image-placeholder.png'),
  
  // Onboarding
  onboarding1: require('./images/onboarding-1.png'),
  onboarding2: require('./images/onboarding-2.png'),
  onboarding3: require('./images/onboarding-3.png'),
};

export const videos = {
  intro: require('./videos/intro.mp4'),
  tutorial: require('./videos/tutorial.mp4'),
};

// Asset helper functions
export const getImageSource = (imageName) => {
  return images[imageName] || images.imagePlaceholder;
};

export const getVideoSource = (videoName) => {
  return videos[videoName];
};

// Image size constants
export const imageSizes = {
  thumbnail: { width: 80, height: 80 },
  small: { width: 150, height: 150 },
  medium: { width: 300, height: 200 },
  large: { width: 400, height: 300 },
  fullWidth: { width: '100%', aspectRatio: 16 / 9 },
};

// Usage in components
import { images, getImageSource, imageSizes } from '../assets';

const AssetExample = () => {
  return (
    <View>
      <Image source={images.logo} style={imageSizes.medium} />
      <Image 
        source={getImageSource('nonexistent')} 
        style={imageSizes.thumbnail} 
      />
    </View>
  );
};
```

### Dynamic Asset Loading

```jsx path=null start=null
// For dynamic image loading based on themes or conditions
const ThemeAwareImage = ({ imageName, theme = 'light', ...props }) => {
  const getThemedImage = (name, currentTheme) => {
    const themeMap = {
      light: {
        logo: require('../assets/images/logo-light.png'),
        background: require('../assets/images/bg-light.png'),
      },
      dark: {
        logo: require('../assets/images/logo-dark.png'),
        background: require('../assets/images/bg-dark.png'),
      },
    };

    return themeMap[currentTheme]?.[name] || themeMap.light[name];
  };

  return (
    <Image
      source={getThemedImage(imageName, theme)}
      {...props}
    />
  );
};

// Responsive image component
const ResponsiveImage = ({ source, maxWidth, aspectRatio = 1, ...props }) => {
  const { width: screenWidth } = Dimensions.get('window');
  
  const imageWidth = Math.min(maxWidth || screenWidth, screenWidth - 40);
  const imageHeight = imageWidth / aspectRatio;

  return (
    <Image
      source={source}
      style={{
        width: imageWidth,
        height: imageHeight,
      }}
      {...props}
    />
  );
};
```

---

## 💡 Key Takeaways

1. **Always specify dimensions** for Image components
2. **Use appropriate resize modes** for different layouts
3. **Handle loading states** and errors gracefully
4. **Optimize images** for better performance
5. **Use FastImage** for better caching and performance
6. **Request permissions** before accessing camera/gallery
7. **Organize assets** in a centralized location
8. **Consider progressive loading** for large images

---

## 🔗 Related Notes
- [[3.Core React Native Components]] - Basic Image component
- [[14.Performance Optimization]] - Image optimization techniques
- [[12.Platform APIs]] - Camera and photo library permissions

## 📚 Next Steps
1. Practice implementing image pickers and galleries
2. Set up proper asset organization system
3. Implement image caching strategies
4. Move on to [[9.Local State Management]]

---
*Tags: #react-native #images #media #assets #stage-2 #camera #video*
